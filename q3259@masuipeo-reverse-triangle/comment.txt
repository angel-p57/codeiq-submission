中心の核 ( nが偶数なら空、奇数なら1要素 ) を元に、外側左右に層を重ねるように動的計画法を行う。
一番下にくる値は、二項係数 (n-1)Ck と、一番上の段の数値の積和で計算できるので、二項係数の対称性を活かすために2個1組で扱う。
選択可能な最小値 minc と、使用済みの数を表すmask ( mincからの差をbit-setにより表現 )、確認済みの外殻の左の列・右の列の数値 ( 一番下の要素は重複する ) を更新しつつ、2個1組の数の和をカウントアップするように探索する
mincに応じて、一番下に来る値の最小値をある程度見積もり、早めに枝刈りできるようにした。
